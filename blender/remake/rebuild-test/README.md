# Reconstruction validity workspace

`test-videos/` contains local generated MP4 inputs and `outputs/` contains derived diagnostics. Both are intentionally ignored by Git.

The reusable implementation lives in `code/docs/reconstruction_mvp.md`; the handoff checklist and one-command runner live in `code/docs/reconstruction_handoff.md`.
